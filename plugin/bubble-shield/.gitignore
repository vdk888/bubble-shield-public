# never ship a deployment's real config (may name real client folders)
config/bubble-shield.json
__pycache__/
*.pyc

# never ship a real deployment firm-identity config (only the .example)
vendor/bubble_shield/deployment_allowlist.json

# Internal R&D positioning doc — never ship to public
docs/Bubble-Shield-Note-Preparation-Deploiement.docx

# Dev-only test suite — never ship to public (synthetic PII fixtures trip the backend content scanner)
scripts/test_*.py
